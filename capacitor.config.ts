import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.brightcapybarasoar',
  appName: 'bright-capybara-soar',
  webDir: 'dist'
};

export default config;
